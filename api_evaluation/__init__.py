"""
api_evaluation package

This file makes the api_evaluation directory importable as a package, so
scripts can use imports like:

  from api_evaluation.utils.deduplicate import _normalize_name

without needing extra sys.path hacks.
"""

