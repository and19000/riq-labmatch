# Frontend

Server-rendered UI assets for RIQ LabMatch.

## Structure

- `templates/` Jinja templates rendered by Flask routes
- `static/` CSS, JavaScript, and image assets

## Notes

The frontend stack is Jinja + vanilla JS (no SPA framework).
